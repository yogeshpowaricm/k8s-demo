{
  "name": "api",
  "version": "1.0.0",
  "description": "API service — forwards compute requests and returns results",
  "main": "main.js",
  "scripts": {
    "start": "node main.js"
  },
  "engines": {
    "node": ">=18"
  }
}
