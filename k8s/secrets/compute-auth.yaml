# Shared token for api → compute service-to-service auth.
# In production, rotate via your secrets manager; never commit real tokens.
apiVersion: v1
kind: Secret
metadata:
  name: compute-auth
  namespace: demo
type: Opaque
stringData:
  token: demo-internal-token-change-me
